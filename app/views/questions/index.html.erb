<h1>Questions</h1>
<%= @questions.each do |question| %>
<p><%= link_to question.title, question %></p>
<p><%= label_tag(:resolved, "Question answered") %> <%= check_box_tag(:resolved) %></p>
<% end %>
